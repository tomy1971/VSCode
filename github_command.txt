git commit -m 'Création initiale du projet'   
git config --global user.name "Réjean Chicoine" 
git config --global user.email "komodo36@hotmail.com"

git log --oneline

git show index.html   montre les fichier html

git config user.name  savoir quel est le nom de la personne.
git config user.email savoir quel est l'email.

git config --global user.name "newemail"  set le nom de la personne
git config --global user.email "newemail@example.com" set le courriel

git remote add origin
git remote add origin url
git remote set-url url

git branch -a  list all branch
git branch module_login will create new branch
git switch 'Module_login'
git branch -D  supprime une branch
git checkout master


git checkout master   retourne a la page master